export interface ICustomer {
    id: number;
    firstName: string;
    lastName: string;
    gender: string;
    address: string;
}

export interface IUserLogin {
    email: string;
    password: string;
}